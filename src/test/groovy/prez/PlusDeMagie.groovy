package prez

import cetitre.BonjourController
import cetitre.BonjourControllerIntegrationSpec
import cetitre.PrincipalController
import cetitre.fip.FipClient

class PlusDeMagie {

    FipClient hop_juste_une_interface

    /**
     * envie de servir en http notre version {@link cetitre.PlayList}
     * plus lisible de l'api Fip {@link cetitre.fip.ReponseLiveMeta}
     * en convertissant la structure
     * {@link cetitre.fip.ConvertisseurLiveMeta#versPlayList(cetitre.fip.ReponseLiveMeta)}
     *
     */

    BonjourController dabord_les_bases_du_serveur // = dis bonjour!

    BonjourControllerIntegrationSpec un_test_dintegration_a_la_mimine

    PrincipalController api_de_fip_ca_donne

    EtLeSql suivant
}
